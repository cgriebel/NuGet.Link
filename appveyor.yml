version: 1.0.{build}
branches:
  only:
  - master
skip_tags: true
install:
  - cd %APPVEYOR_BUILD_FOLDER%
  - git submodule update --init --recursive
before_build:
  - nuget restore
build:
  project: Nuget.Link.sln
  verbosity: detailed